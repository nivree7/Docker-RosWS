
sudo apt install ros-humble-xacro ros-humble-ros-ign-gazebo ros-humble-ros-ign-bridge
ASSUMING ROS2 and Fortress installed
from ~/ros2_ws
{
colcon build --packages-select wheeled_robot


source install/setup.bash
your could set it up in your bash rc so you never have to worry about sourcing it


ros2 launch wheeled_robot diff_drive_launch.py world:=empty.sdf x:=1.0 y:=2.0 z:=0.5 roll:=0.0 pitch:=0.0 yaw:=1.57

}
if it doesnt spawn in, god help you 🙏

The instructions are written in write only language thus they are not included

here is a link to the repo i used as a guide https://github.com/lucasmazzetto/gazebo_differential_drive_robot
https://www.youtube.com/watch?v=dZ_CyyEvBE0

checking if working gazebo 
ign topic -l | grep differential_drive_robot
 checking in ros 
 ros2  topic list